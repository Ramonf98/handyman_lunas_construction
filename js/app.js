// import { allTranslations } from './translations.js';

// let currentPageLang = 'en';

// document.getElementById('lang_picker').addEventListener('click', () => {
//     currentPageLang = currentPageLang == 'en' ? 'es' : 'en';

//     const translations = allTranslations[currentPageLang];
//     const translationKeys = Object.keys(translations);

//     translationKeys.forEach((elementID) => {
//         console.log(elementID);
//         const translatedText = translations[elementID];
//         const htmlElement = document.getElementById(elementID);
//         if (!htmlElement) return;
//         htmlElement.innerHTML = translatedText;
//     });
// });

// console.log(screen.width);
